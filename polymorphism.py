name = "Dipesh Paudel"  # str
print(len(name))

some_list = ["some", "list"]  # list
print(len(some_list))
# That's polymorphism in action, a single function does now
# How to handle different kinds of objects as expected
